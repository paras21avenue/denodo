CREATE OR REPLACE INTERFACE VIEW sales (
        employeeid:text,
        productid:text,
        customerid:text,
        saleid:text,
        saledate:text,
        bundleamount:text,
        productsaleprice:text,
        totalprice:text
    )
    SET IMPLEMENTATION sales_dummy
    FOLDER = '/importedmodels';

