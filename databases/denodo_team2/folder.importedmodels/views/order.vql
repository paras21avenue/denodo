CREATE OR REPLACE INTERFACE VIEW order (
        customer_code:text,
        product_id:decimal,
        id:decimal,
        status:text,
        date_placed:localdate,
        date_delivered:localdate,
        date_closed:localdate
    )
    SET IMPLEMENTATION order_dummy
    FOLDER = '/importedmodels';

